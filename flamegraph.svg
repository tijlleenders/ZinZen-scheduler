<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="470" onload="init(evt)" viewBox="0 0 1200 470" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="470" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="453.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="453.00"> </text><svg id="frames" x="10" width="1180" total_samples="36"><g><title>[anon] (1 samples, 2.78%)</title><rect x="0.0000%" y="389" width="2.7778%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="1"/><text x="0.2500%" y="399.50">[a..</text></g><g><title>_ZN58_$LT$alloc..string..String$u20$as$u20$core..fmt..Write$GT$9write_str17h2ba0f20a474d1c4aE.480 (1 samples, 2.78%)</title><rect x="0.0000%" y="373" width="2.7778%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="1"/><text x="0.2500%" y="383.50">_Z..</text></g><g><title>[unknown] (1 samples, 2.78%)</title><rect x="2.7778%" y="389" width="2.7778%" height="15" fill="rgb(221,193,54)" fg:x="1" fg:w="1"/><text x="3.0278%" y="399.50">[u..</text></g><g><title>_int_free (1 samples, 2.78%)</title><rect x="2.7778%" y="373" width="2.7778%" height="15" fill="rgb(248,212,6)" fg:x="1" fg:w="1"/><text x="3.0278%" y="383.50">_i..</text></g><g><title>__memmove_avx_unaligned_erms (1 samples, 2.78%)</title><rect x="5.5556%" y="293" width="2.7778%" height="15" fill="rgb(208,68,35)" fg:x="2" fg:w="1"/><text x="5.8056%" y="303.50">__..</text></g><g><title>scheduler::models::activity::Activity::get_budget_min_day_activities (1 samples, 2.78%)</title><rect x="11.1111%" y="229" width="2.7778%" height="15" fill="rgb(232,128,0)" fg:x="4" fg:w="1"/><text x="11.3611%" y="239.50">sc..</text></g><g><title>scheduler::models::activity::Activity::get_compatible_hours_overlay (1 samples, 2.78%)</title><rect x="11.1111%" y="213" width="2.7778%" height="15" fill="rgb(207,160,47)" fg:x="4" fg:w="1"/><text x="11.3611%" y="223.50">sc..</text></g><g><title>scheduler::models::calendar::Calendar::get_index_of (1 samples, 2.78%)</title><rect x="11.1111%" y="197" width="2.7778%" height="15" fill="rgb(228,23,34)" fg:x="4" fg:w="1"/><text x="11.3611%" y="207.50">sc..</text></g><g><title>chrono::naive::datetime::NaiveDateTime::signed_duration_since (1 samples, 2.78%)</title><rect x="11.1111%" y="181" width="2.7778%" height="15" fill="rgb(218,30,26)" fg:x="4" fg:w="1"/><text x="11.3611%" y="191.50">ch..</text></g><g><title>scheduler::models::activity::Activity::get_compatible_hours_overlay (1 samples, 2.78%)</title><rect x="13.8889%" y="229" width="2.7778%" height="15" fill="rgb(220,122,19)" fg:x="5" fg:w="1"/><text x="14.1389%" y="239.50">sc..</text></g><g><title>__memcmp_avx2_movbe (3 samples, 8.33%)</title><rect x="27.7778%" y="213" width="8.3333%" height="15" fill="rgb(250,228,42)" fg:x="10" fg:w="3"/><text x="28.0278%" y="223.50">__memcmp_avx..</text></g><g><title>&lt;alloc::rc::Rc&lt;T,A&gt; as core::ops::drop::Drop&gt;::drop (2 samples, 5.56%)</title><rect x="41.6667%" y="197" width="5.5556%" height="15" fill="rgb(240,193,28)" fg:x="15" fg:w="2"/><text x="41.9167%" y="207.50">&lt;alloc:..</text></g><g><title>scheduler::models::activity::Activity::flex (6 samples, 16.67%)</title><rect x="36.1111%" y="213" width="16.6667%" height="15" fill="rgb(216,20,37)" fg:x="13" fg:w="6"/><text x="36.3611%" y="223.50">scheduler::models::activit..</text></g><g><title>cfree@GLIBC_2.2.5 (2 samples, 5.56%)</title><rect x="47.2222%" y="197" width="5.5556%" height="15" fill="rgb(206,188,39)" fg:x="17" fg:w="2"/><text x="47.4722%" y="207.50">cfree@G..</text></g><g><title>_int_free (2 samples, 5.56%)</title><rect x="47.2222%" y="181" width="5.5556%" height="15" fill="rgb(217,207,13)" fg:x="17" fg:w="2"/><text x="47.4722%" y="191.50">_int_fr..</text></g><g><title>cfree@GLIBC_2.2.5 (1 samples, 2.78%)</title><rect x="55.5556%" y="149" width="2.7778%" height="15" fill="rgb(231,73,38)" fg:x="20" fg:w="1"/><text x="55.8056%" y="159.50">cf..</text></g><g><title>_int_free (1 samples, 2.78%)</title><rect x="55.5556%" y="133" width="2.7778%" height="15" fill="rgb(225,20,46)" fg:x="20" fg:w="1"/><text x="55.8056%" y="143.50">_i..</text></g><g><title>core::fmt::num::imp::&lt;impl core::fmt::Display for u64&gt;::fmt (1 samples, 2.78%)</title><rect x="58.3333%" y="149" width="2.7778%" height="15" fill="rgb(210,31,41)" fg:x="21" fg:w="1"/><text x="58.5833%" y="159.50">co..</text></g><g><title>_ZN58_$LT$alloc..string..String$u20$as$u20$core..fmt..Write$GT$9write_str17h2ba0f20a474d1c4aE.480 (1 samples, 2.78%)</title><rect x="58.3333%" y="133" width="2.7778%" height="15" fill="rgb(221,200,47)" fg:x="21" fg:w="1"/><text x="58.5833%" y="143.50">_Z..</text></g><g><title>alloc::raw_vec::RawVec&lt;T,A&gt;::reserve::do_reserve_and_handle (1 samples, 2.78%)</title><rect x="58.3333%" y="117" width="2.7778%" height="15" fill="rgb(226,26,5)" fg:x="21" fg:w="1"/><text x="58.5833%" y="127.50">al..</text></g><g><title>alloc::raw_vec::RawVec&lt;T,A&gt;::grow_amortized (1 samples, 2.78%)</title><rect x="58.3333%" y="101" width="2.7778%" height="15" fill="rgb(249,33,26)" fg:x="21" fg:w="1"/><text x="58.5833%" y="111.50">al..</text></g><g><title>alloc::raw_vec::finish_grow (1 samples, 2.78%)</title><rect x="58.3333%" y="85" width="2.7778%" height="15" fill="rgb(235,183,28)" fg:x="21" fg:w="1"/><text x="58.5833%" y="95.50">al..</text></g><g><title>malloc (1 samples, 2.78%)</title><rect x="58.3333%" y="69" width="2.7778%" height="15" fill="rgb(221,5,38)" fg:x="21" fg:w="1"/><text x="58.5833%" y="79.50">ma..</text></g><g><title>&lt;[T] as core::fmt::Debug&gt;::fmt (1 samples, 2.78%)</title><rect x="61.1111%" y="101" width="2.7778%" height="15" fill="rgb(247,18,42)" fg:x="22" fg:w="1"/><text x="61.3611%" y="111.50">&lt;[..</text></g><g><title>core::fmt::builders::DebugSet::entry (1 samples, 2.78%)</title><rect x="61.1111%" y="85" width="2.7778%" height="15" fill="rgb(241,131,45)" fg:x="22" fg:w="1"/><text x="61.3611%" y="95.50">co..</text></g><g><title>&lt;&amp;T as core::fmt::Debug&gt;::fmt (1 samples, 2.78%)</title><rect x="61.1111%" y="69" width="2.7778%" height="15" fill="rgb(249,31,29)" fg:x="22" fg:w="1"/><text x="61.3611%" y="79.50">&lt;&amp;..</text></g><g><title>core::fmt::write (1 samples, 2.78%)</title><rect x="61.1111%" y="53" width="2.7778%" height="15" fill="rgb(225,111,53)" fg:x="22" fg:w="1"/><text x="61.3611%" y="63.50">co..</text></g><g><title>core::fmt::num::imp::&lt;impl core::fmt::Display for u64&gt;::fmt (1 samples, 2.78%)</title><rect x="61.1111%" y="37" width="2.7778%" height="15" fill="rgb(238,160,17)" fg:x="22" fg:w="1"/><text x="61.3611%" y="47.50">co..</text></g><g><title>scheduler::run_scheduler (23 samples, 63.89%)</title><rect x="8.3333%" y="245" width="63.8889%" height="15" fill="rgb(214,148,48)" fg:x="3" fg:w="23"/><text x="8.5833%" y="255.50">scheduler::run_scheduler</text></g><g><title>scheduler::services::activity_placer::place (20 samples, 55.56%)</title><rect x="16.6667%" y="229" width="55.5556%" height="15" fill="rgb(232,36,49)" fg:x="6" fg:w="20"/><text x="16.9167%" y="239.50">scheduler::services::activity_placer::place</text></g><g><title>std::io::stdio::_eprint (7 samples, 19.44%)</title><rect x="52.7778%" y="213" width="19.4444%" height="15" fill="rgb(209,103,24)" fg:x="19" fg:w="7"/><text x="53.0278%" y="223.50">std::io::stdio::_eprint</text></g><g><title>std::io::stdio::print_to_buffer_if_capture_used (7 samples, 19.44%)</title><rect x="52.7778%" y="197" width="19.4444%" height="15" fill="rgb(229,88,8)" fg:x="19" fg:w="7"/><text x="53.0278%" y="207.50">std::io::stdio::print_to_buffe..</text></g><g><title>core::fmt::write (7 samples, 19.44%)</title><rect x="52.7778%" y="181" width="19.4444%" height="15" fill="rgb(213,181,19)" fg:x="19" fg:w="7"/><text x="53.0278%" y="191.50">core::fmt::write</text></g><g><title>&lt;scheduler::models::calendar::Calendar as core::fmt::Debug&gt;::fmt (7 samples, 19.44%)</title><rect x="52.7778%" y="165" width="19.4444%" height="15" fill="rgb(254,191,54)" fg:x="19" fg:w="7"/><text x="53.0278%" y="175.50">&lt;scheduler::models::calendar::..</text></g><g><title>core::fmt::write (4 samples, 11.11%)</title><rect x="61.1111%" y="149" width="11.1111%" height="15" fill="rgb(241,83,37)" fg:x="22" fg:w="4"/><text x="61.3611%" y="159.50">core::fmt::write</text></g><g><title>&lt;&amp;T as core::fmt::Debug&gt;::fmt (4 samples, 11.11%)</title><rect x="61.1111%" y="133" width="11.1111%" height="15" fill="rgb(233,36,39)" fg:x="22" fg:w="4"/><text x="61.3611%" y="143.50">&lt;&amp;T as core::fmt..</text></g><g><title>core::fmt::builders::DebugStruct::field (4 samples, 11.11%)</title><rect x="61.1111%" y="117" width="11.1111%" height="15" fill="rgb(226,3,54)" fg:x="22" fg:w="4"/><text x="61.3611%" y="127.50">core::fmt::build..</text></g><g><title>&lt;alloc::vec::Vec&lt;T,A&gt; as core::fmt::Debug&gt;::fmt (3 samples, 8.33%)</title><rect x="63.8889%" y="101" width="8.3333%" height="15" fill="rgb(245,192,40)" fg:x="23" fg:w="3"/><text x="64.1389%" y="111.50">&lt;alloc::vec:..</text></g><g><title>core::fmt::builders::DebugSet::entry (3 samples, 8.33%)</title><rect x="63.8889%" y="85" width="8.3333%" height="15" fill="rgb(238,167,29)" fg:x="23" fg:w="3"/><text x="64.1389%" y="95.50">core::fmt::b..</text></g><g><title>&lt;str as core::fmt::Debug&gt;::fmt (3 samples, 8.33%)</title><rect x="63.8889%" y="69" width="8.3333%" height="15" fill="rgb(232,182,51)" fg:x="23" fg:w="3"/><text x="64.1389%" y="79.50">&lt;str as core..</text></g><g><title>&lt;std::io::Write::write_fmt::Adapter&lt;T&gt; as core::fmt::Write&gt;::write_str (1 samples, 2.78%)</title><rect x="69.4444%" y="53" width="2.7778%" height="15" fill="rgb(231,60,39)" fg:x="25" fg:w="1"/><text x="69.6944%" y="63.50">&lt;s..</text></g><g><title>serde::de::impls::&lt;impl serde::de::Deserialize for core::option::Option&lt;T&gt;&gt;::deserialize (1 samples, 2.78%)</title><rect x="72.2222%" y="229" width="2.7778%" height="15" fill="rgb(208,69,12)" fg:x="26" fg:w="1"/><text x="72.4722%" y="239.50">se..</text></g><g><title>serde::de::impls::&lt;impl serde::de::Deserialize for alloc::vec::Vec&lt;T&gt;&gt;::deserialize (1 samples, 2.78%)</title><rect x="72.2222%" y="213" width="2.7778%" height="15" fill="rgb(235,93,37)" fg:x="26" fg:w="1"/><text x="72.4722%" y="223.50">se..</text></g><g><title>serde_json::de::Deserializer&lt;R&gt;::parse_whitespace (1 samples, 2.78%)</title><rect x="72.2222%" y="197" width="2.7778%" height="15" fill="rgb(213,116,39)" fg:x="26" fg:w="1"/><text x="72.4722%" y="207.50">se..</text></g><g><title>serde_json::de::Deserializer&lt;R&gt;::peek (1 samples, 2.78%)</title><rect x="72.2222%" y="181" width="2.7778%" height="15" fill="rgb(222,207,29)" fg:x="26" fg:w="1"/><text x="72.4722%" y="191.50">se..</text></g><g><title>e2e::tijl_goals (28 samples, 77.78%)</title><rect x="0.0000%" y="405" width="77.7778%" height="15" fill="rgb(206,96,30)" fg:x="0" fg:w="28"/><text x="0.2500%" y="415.50">e2e::tijl_goals</text></g><g><title>clone3 (26 samples, 72.22%)</title><rect x="5.5556%" y="389" width="72.2222%" height="15" fill="rgb(218,138,4)" fg:x="2" fg:w="26"/><text x="5.8056%" y="399.50">clone3</text></g><g><title>start_thread (26 samples, 72.22%)</title><rect x="5.5556%" y="373" width="72.2222%" height="15" fill="rgb(250,191,14)" fg:x="2" fg:w="26"/><text x="5.8056%" y="383.50">start_thread</text></g><g><title>std::sys::pal::unix::thread::Thread::new::thread_start (26 samples, 72.22%)</title><rect x="5.5556%" y="357" width="72.2222%" height="15" fill="rgb(239,60,40)" fg:x="2" fg:w="26"/><text x="5.8056%" y="367.50">std::sys::pal::unix::thread::Thread::new::thread_start</text></g><g><title>core::ops::function::FnOnce::call_once{{vtable.shim}} (26 samples, 72.22%)</title><rect x="5.5556%" y="341" width="72.2222%" height="15" fill="rgb(206,27,48)" fg:x="2" fg:w="26"/><text x="5.8056%" y="351.50">core::ops::function::FnOnce::call_once{{vtable.shim}}</text></g><g><title>std::sys::backtrace::__rust_begin_short_backtrace (26 samples, 72.22%)</title><rect x="5.5556%" y="325" width="72.2222%" height="15" fill="rgb(225,35,8)" fg:x="2" fg:w="26"/><text x="5.8056%" y="335.50">std::sys::backtrace::__rust_begin_short_backtrace</text></g><g><title>test::run_test::_{{closure}} (26 samples, 72.22%)</title><rect x="5.5556%" y="309" width="72.2222%" height="15" fill="rgb(250,213,24)" fg:x="2" fg:w="26"/><text x="5.8056%" y="319.50">test::run_test::_{{closure}}</text></g><g><title>test::__rust_begin_short_backtrace (25 samples, 69.44%)</title><rect x="8.3333%" y="293" width="69.4444%" height="15" fill="rgb(247,123,22)" fg:x="3" fg:w="25"/><text x="8.5833%" y="303.50">test::__rust_begin_short_backtrace</text></g><g><title>core::ops::function::FnOnce::call_once (25 samples, 69.44%)</title><rect x="8.3333%" y="277" width="69.4444%" height="15" fill="rgb(231,138,38)" fg:x="3" fg:w="25"/><text x="8.5833%" y="287.50">core::ops::function::FnOnce::call_once</text></g><g><title>rust_tests::e2e::test (25 samples, 69.44%)</title><rect x="8.3333%" y="261" width="69.4444%" height="15" fill="rgb(231,145,46)" fg:x="3" fg:w="25"/><text x="8.5833%" y="271.50">rust_tests::e2e::test</text></g><g><title>serde::de::impls::&lt;impl serde::de::Deserialize for alloc::vec::Vec&lt;T&gt;&gt;::deserialize (2 samples, 5.56%)</title><rect x="72.2222%" y="245" width="5.5556%" height="15" fill="rgb(251,118,11)" fg:x="26" fg:w="2"/><text x="72.4722%" y="255.50">serde::..</text></g><g><title>serde_json::de::Deserializer&lt;R&gt;::parse_whitespace (1 samples, 2.78%)</title><rect x="75.0000%" y="229" width="2.7778%" height="15" fill="rgb(217,147,25)" fg:x="27" fg:w="1"/><text x="75.2500%" y="239.50">se..</text></g><g><title>__libc_start_main@@GLIBC_2.34 (1 samples, 2.78%)</title><rect x="91.6667%" y="373" width="2.7778%" height="15" fill="rgb(247,81,37)" fg:x="33" fg:w="1"/><text x="91.9167%" y="383.50">__..</text></g><g><title>__libc_start_call_main (1 samples, 2.78%)</title><rect x="91.6667%" y="357" width="2.7778%" height="15" fill="rgb(209,12,38)" fg:x="33" fg:w="1"/><text x="91.9167%" y="367.50">__..</text></g><g><title>main (1 samples, 2.78%)</title><rect x="91.6667%" y="341" width="2.7778%" height="15" fill="rgb(227,1,9)" fg:x="33" fg:w="1"/><text x="91.9167%" y="351.50">ma..</text></g><g><title>std::sys::backtrace::__rust_begin_short_backtrace (1 samples, 2.78%)</title><rect x="91.6667%" y="325" width="2.7778%" height="15" fill="rgb(248,47,43)" fg:x="33" fg:w="1"/><text x="91.9167%" y="335.50">st..</text></g><g><title>rust_tests::main (1 samples, 2.78%)</title><rect x="91.6667%" y="309" width="2.7778%" height="15" fill="rgb(221,10,30)" fg:x="33" fg:w="1"/><text x="91.9167%" y="319.50">ru..</text></g><g><title>test::test_main (1 samples, 2.78%)</title><rect x="91.6667%" y="293" width="2.7778%" height="15" fill="rgb(210,229,1)" fg:x="33" fg:w="1"/><text x="91.9167%" y="303.50">te..</text></g><g><title>test::term::terminfo::TerminfoTerminal&lt;T&gt;::new (1 samples, 2.78%)</title><rect x="91.6667%" y="277" width="2.7778%" height="15" fill="rgb(222,148,37)" fg:x="33" fg:w="1"/><text x="91.9167%" y="287.50">te..</text></g><g><title>core::iter::adapters::try_process (1 samples, 2.78%)</title><rect x="91.6667%" y="261" width="2.7778%" height="15" fill="rgb(234,67,33)" fg:x="33" fg:w="1"/><text x="91.9167%" y="271.50">co..</text></g><g><title>_start (7 samples, 19.44%)</title><rect x="77.7778%" y="389" width="19.4444%" height="15" fill="rgb(247,98,35)" fg:x="28" fg:w="7"/><text x="78.0278%" y="399.50">_start</text></g><g><title>_dl_start (1 samples, 2.78%)</title><rect x="94.4444%" y="373" width="2.7778%" height="15" fill="rgb(247,138,52)" fg:x="34" fg:w="1"/><text x="94.6944%" y="383.50">_d..</text></g><g><title>_dl_sysdep_start (1 samples, 2.78%)</title><rect x="94.4444%" y="357" width="2.7778%" height="15" fill="rgb(213,79,30)" fg:x="34" fg:w="1"/><text x="94.6944%" y="367.50">_d..</text></g><g><title>__GI___tunables_init (1 samples, 2.78%)</title><rect x="94.4444%" y="341" width="2.7778%" height="15" fill="rgb(246,177,23)" fg:x="34" fg:w="1"/><text x="94.6944%" y="351.50">__..</text></g><g><title>all (36 samples, 100%)</title><rect x="0.0000%" y="421" width="100.0000%" height="15" fill="rgb(230,62,27)" fg:x="0" fg:w="36"/><text x="0.2500%" y="431.50"></text></g><g><title>rust_tests-0ad0 (8 samples, 22.22%)</title><rect x="77.7778%" y="405" width="22.2222%" height="15" fill="rgb(216,154,8)" fg:x="28" fg:w="8"/><text x="78.0278%" y="415.50">rust_tests-0ad0</text></g><g><title>clone3 (1 samples, 2.78%)</title><rect x="97.2222%" y="389" width="2.7778%" height="15" fill="rgb(244,35,45)" fg:x="35" fg:w="1"/><text x="97.4722%" y="399.50">cl..</text></g><g><title>start_thread (1 samples, 2.78%)</title><rect x="97.2222%" y="373" width="2.7778%" height="15" fill="rgb(251,115,12)" fg:x="35" fg:w="1"/><text x="97.4722%" y="383.50">st..</text></g><g><title>__ctype_init (1 samples, 2.78%)</title><rect x="97.2222%" y="357" width="2.7778%" height="15" fill="rgb(240,54,50)" fg:x="35" fg:w="1"/><text x="97.4722%" y="367.50">__..</text></g></svg></svg>